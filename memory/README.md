# JARVIS memory folder

JARVIS saves what you tell it to remember here as `assistant-memory.md`.

Open **this folder** in Obsidian (or any text editor) to read and edit your memories.

You don't need to create anything — when you say "remember that..." JARVIS makes
the file for you. Point `VAULT_DIR` in your `.env` at this folder and you're set.
